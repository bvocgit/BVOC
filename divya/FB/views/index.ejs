<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Feedback Form</title>
    <!-- <link rel="stylesheet" href="/css/styles.css"> -->

    <style>
body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background-color: #f0f4f8;
    margin: 0;
    padding: 0;
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 100vh;
}

/* Styling for form container */
.form-container {
    background-color: white;
    padding: 40px;
    border-radius: 15px;
    box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
    max-width: 500px;
    width: 100%;
    margin: 20px;
    text-align: center;
}

/* Form heading */
.form-container h1 {
    font-size: 24px;
    color: #333;
    margin-bottom: 20px;
}

/* Styling for labels */
label {
    font-size: 14px;
    color: #666;
    display: block;
    margin-bottom: 5px;
}

/* Styling for inputs, textareas, and select fields */
input[type="text"],
input[type="email"],
textarea,
select {
    width: 100%;
    padding: 12px;
    margin: 8px 0 16px 0;
    display: block;
    border: 1px solid #ccc;
    border-radius: 15px;
    background-color: #f8f9fa;
    font-size: 14px;
    color: #333;
    box-sizing: border-box;
    transition: border-color 0.3s ease;
}

/* Focus styling for inputs and textarea */
input[type="text"]:focus,
input[type="email"]:focus,
textarea:focus,
select:focus {
    border-color: #3700ff;
    outline: none;
}

/* Styling for buttons */
button.submit-btn {
    background-color: #007bff;
    color: white;
    border: none;
    padding: 12px 20px;
    border-radius: 20px;
    font-size: 16px;
    cursor: pointer;
    transition: background-color 0.3s ease;
    width: 100%;
}

button.submit-btn:hover {
    background-color: #0c00b3;
}

/* Responsive adjustments for mobile screens */
@media (max-width: 768px) {
    .form-container {
        padding: 30px;
    }

    .form-container h1 {
        font-size: 22px;
    }

    input[type="text"],
    input[type="email"],
    textarea,
    select,
    button.submit-btn {
        font-size: 14px;
        padding: 10px;
    }
}

/* Extra small devices */
@media (max-width: 576px) {
    .form-container {
        padding: 20px;
    }

    .form-container h1 {
        font-size: 20px;
    }

    input[type="text"],
    input[type="email"],
    textarea,
    select,
    button.submit-btn {
        font-size: 13px;
        padding: 8px;
    }
}

/* Aesthetic additions */
.form-container select,
.form-container input[type="text"],
.form-container input[type="email"],
.form-container textarea {
    background-color: #f9fbfd;
    border: 1px solid #ced4da;
}

.form-container select:hover,
.form-container input[type="text"]:hover,
.form-container input[type="email"]:hover,
.form-container textarea:hover {
    border-color: #007bff;
}

input::placeholder,
textarea::placeholder {
    color: #888;
}

    </style>
</head>
<body>
    <h1><%=text %> </h1>
    <div class="form-container">
        <h1>Submit Your Feedback</h1>
        <form action="/submit-feedback" method="POST">
            <label for="category"> User Category:</label>
            <select name="category" required>
                <option value="select">Select</option>
                <option value="student">Student</option>
                <option value="employee">Employee</option>
                <option value="client">Client</option>
                <option value="Teacher">Teacher</option>
            </select>
            <br>

            <label for="confidential">Is this feedback confidential?</label>
            <select name="confidential" required>
                <option value="non-confidential">Non-Confidential</option>
                <option value="confidential">Confidential</option>
            </select>
            <br>

            <div id="nonConfidentialDetails">
                <label for="name">Name:</label>
                <input type="text" name="name">
                <br><br>
                <label for="email">Email:</label>
                <input type="email" name="email">
            </div>
            <br>

            <label for="feedback">Your Feedback:</label>
            <textarea name="feedback" rows="4" required></textarea>
            <br>

            <button type="submit" class="submit-btn">Submit Feedback</button>
        </form>
    </div>

    <script>
        // Ensure DOM is loaded before running JavaScript
        document.addEventListener('DOMContentLoaded', function () {
            const confidentialSelect = document.querySelector('select[name="confidential"]');
            const nonConfidentialDetails = document.getElementById('nonConfidentialDetails');

            // Add event listener to show/hide non-confidential details based on selection
            confidentialSelect.addEventListener('change', function() {
                if (this.value === 'confidential') {
                    nonConfidentialDetails.style.display = 'none';
                } else {
                    nonConfidentialDetails.style.display = 'block';
                }
            });
        });
    </script>
</body>
</html>
